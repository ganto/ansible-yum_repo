---

# EPEL release package URL
epel_repo_url: 'https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ ansible_distribution_major_version }}.noarch.rpm'

# EPEL gpg key file path
epel_repo_gpg_key_url: '/etc/pki/rpm-gpg/RPM-GPG-KEY-EPEL-{{ ansible_distribution_major_version }}'

# EPEL repository file
epel_repo_file_path: '/etc/yum.repos.d/epel.repo'

# EPEL repository URLs
epel_repo_baseurls:
  epel: 'http://pkgs.{{ ansible_domain }}/epel/$releasever/$basearch'
  epel-debuginfo: 'http://pkgs.{{ ansible_domain }}/epel/$releasever/$basearch/debug'
  epel-source: 'http://pkgs.{{ ansible_domain }}/epel/$releasever/$basearch/SRPMS'
